<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.jacorycyjin.pinhub.backend.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="io.github.jacorycyjin.pinhub.backend.po.UserPO">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="nickname" property="nickname" />
        <result column="avatar_url" property="avatarUrl" />
        <result column="status" property="status" />
        <result column="deleted" property="deleted" />
        <result column="ctime" property="ctime" />
        <result column="mtime" property="mtime" />
    </resultMap>

    <!-- 查询用户 -->
    <select id="findUser" resultMap="UserResultMap"> SELECT * FROM user WHERE deleted = 0 and status
        = 0 and user_id = #{userId} </select>

</mapper>